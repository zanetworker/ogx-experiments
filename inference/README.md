# Inference

| Script | What it tests |
|--------|--------------|
| `check_models.py` | List registered models via HTTP |
| `simple_inference.py` | Responses API with streaming and non-streaming |
| `token_tracking.py` | Token usage tracking on inference calls |
| `structured_output.py` | JSON schema response format (strings, nested, enums, arrays) |
| `embeddings.py` | Embedding creation and dimension inspection |
