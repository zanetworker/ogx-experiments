# Multi-SDK

| Script | What it tests |
|--------|--------------|
| `anthropic_messages.py` | Anthropic Messages API via OGX (`/v1/messages`) |
| `gemini_interactions.py` | Gemini Interactions API via OGX (`/v1alpha/interactions`) |

Requires `anthropic` and `google-genai` packages (in requirements.txt).
